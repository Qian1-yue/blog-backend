spring:
  config:
    activate:
      on-profile: local

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.example.blogbackend: DEBUG
